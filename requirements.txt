# Core
numpy>=1.26,<3.0

# PyTorch stack (CPU on macOS by default; choose CUDA wheel explicitly on Linux if desired)
torch>=2.2,<3.0
torchvision>=0.17,<1.0

# Utilities
tqdm>=4.66,<5.0
matplotlib>=3.8,<4.0
pillow>=10.0,<12.0
# Optional: nicer image grids / saving
opencv-python>=4.8,<5.0

# Optional: notebooks
jupyter>=1.0,<2.0
ipykernel>=6.0,<7.0

